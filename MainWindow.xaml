<Window x:Class="TakenncsUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        ResizeMode="CanResize"
        AllowsTransparency="True"
        Background="Transparent"
        Width="1000"
        Height="500">

    <Border Background="{StaticResource BgDarkBrush}"
            CornerRadius="12"
            Padding="12">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="{StaticResource BgDarkBrush}"
                    CornerRadius="10"
                    Margin="0,0,12,0">

                <Grid RowDefinitions="40,*">
                    <Grid Background="Transparent"
                          MouseLeftButtonDown="Header_MouseLeftButtonDown">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="40"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="takenncsUI"
                                   VerticalAlignment="Center"
                                   FontSize="16"
                                   Margin="10,0"/>

                        <Button Grid.Column="1"
                                Content="✕"
                                Background="Transparent"
                                Foreground="White"
                                Click="Close_Click"/>
                    </Grid>

                    <Border Grid.Row="1"
                            Margin="0,12,0,0"
                            Background="{StaticResource BgCardBrush}"
                            CornerRadius="10"
                            Padding="20">

                        <StackPanel>
                            <TextBlock Text="WSG 👋"
                                       FontSize="22"
                                       FontWeight="Bold"/>

                            <TextBlock Text="WPF Custom Modern UI"
                                       Margin="0,6,0,20"
                                       Foreground="{StaticResource TextSecondaryBrush}"/>

                            <Button Content="Peamine tegevus"
                                    Width="200"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Column="1"
                    Background="{StaticResource BgCardBrush}"
                    CornerRadius="10"
                    Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0">
                        <Rectangle Height="1"
                                   Margin="0,8,0,16"
                                   Fill="{StaticResource AccentBrush}"
                                   Opacity="0.3"/>
                    </StackPanel>

                    <StackPanel Grid.Row="2">
                        <TextBlock Text="Created by takenncs"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,8"/>

                        <Border Background="{StaticResource BgDarkBrush}"
                                CornerRadius="8"
                                Padding="12"
                                Margin="0,0,0,10">
                            <TextBlock Text="📌 Free for use and editing"
                                       FontSize="12"
                                       TextWrapping="Wrap"/>
                        </Border>

                        <Border Background="{StaticResource BgDarkBrush}"
                                CornerRadius="8"
                                Padding="12"
                                Margin="0,0,0,10">
                            <TextBlock Text="💡 https://github.com/takennncs"
                                       FontSize="12"
                                       TextWrapping="Wrap"/>
                        </Border>
                    </StackPanel>

                    <Border Grid.Row="3"
                            Background="{StaticResource BgDarkBrush}"
                            CornerRadius="8"
                            Padding="12"
                            Margin="0,20,0,0">
                        <StackPanel>
                            <TextBlock Text="Süsteemi staatus"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,4"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Ellipse Width="8"
                                         Height="8"
                                         Fill="LimeGreen"
                                         Margin="0,0,6,0"
                                         VerticalAlignment="Center"/>

                                <TextBlock Grid.Column="1"
                                           Text="Kõik süsteemid töötavad"
                                           FontSize="11"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
